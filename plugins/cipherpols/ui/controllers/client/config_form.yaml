# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Client

# Model Form Field configuration
form: $/cipherpols/ui/models/client/fields.yaml

# Model Class name
modelClass: Cipherpols\Ui\Models\Client

# Default redirect location
defaultRedirect: cipherpols/ui/client

# Create page
create:
    title: Create Client
    redirect: cipherpols/ui/client/update/:id
    redirectClose: cipherpols/ui/client

# Update page
update:
    title: Edit Client
    redirect: cipherpols/ui/client
    redirectClose: cipherpols/ui/client

# Preview page
preview:
    title: Preview Client